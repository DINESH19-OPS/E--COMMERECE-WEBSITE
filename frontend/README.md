# Lumina Frontend (Vite + React)

Quick scaffold for the requested immersive page. To run locally:

1. cd frontend
2. npm install
3. npm run dev

This will start Vite on the default port (5173). The page uses Tailwind CSS (configured), `framer-motion` for animations and `lucide-react` for icons.
